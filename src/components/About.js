import React, { Component } from 'react';
import '../public/css/main.css';

export class About extends Component {
  render() {
    return (
      <div>
        <div id="aboutcontent">
          <p>
            The Store manager is the store's primary key-holder and may be called to the store before, during,
            or after business hours in the event of an emergency. They are also responsible for the safety of
            all customers and employees on store premises. Store managers may be required to hold safety meetings, 
            especially as dictated by union practices in cases where store employees belong to a union.
          </p>
        </div>
      </div>
    )
  }
}

export default About
